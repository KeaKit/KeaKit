describe('Dummy Test TS', () => {
  it('suma correctamente 2 + 2', () => {
    const result = 2 + 2;
    expect(result).toBe(4);
  });

  it('siempre pasa', () => {
    const truth = true;
    expect(truth).toBe(true);
    
  });
});